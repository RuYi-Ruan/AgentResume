# 实验方案 v3：验证 "能力提升后行为—意图映射漂移" 的动机命题（Overcooked）

版本：v3（v3 变更：经验提取显式对齐赛道 D 文献路线；按评审补齐标签粒度、评估协议、稀疏回退、效应复制等；评审记录见文末）
状态：架构已锁定 —— Alice 采用 **经验即上下文（memory-context，= 赛道 D"轨迹→可复用经验/上下文"的可解释实例）**，零/近零训练
范围：**只做 Motivation 验证**，不构建完整 Resume、不训练世界模型
产出：受控实验证据 + 带意图标注、可复用的分层轨迹数据集（为后续世界模型反推意图阶段预留接口）

---

## 0. 要验证的动机命题

> 伙伴 Alice 的能力随时间提升后，外部观察者（Bob）若仍以能力提升前的"印象/经验"解读其行为，会系统性误判行为背后的意图——即使 Alice 的**可观测行为与升级前相同或高度相似**。

- **P1（存在性）**：相同（状态/行为前缀）条件下，Alice 意图的条件分布随其能力层级改变——"同样的行为，意图变了"可观测、可量化。
- **P2（滞后伤害 + 更新恢复）**：Bob 用陈旧经验推断会系统性失准；持续摄入新经验后准确率恢复。

## 1. 角色与总体架构

| 角色 | 定义 | 实现（本阶段） |
|---|---|---|
| Alice | 被观察伙伴；**能力随自身经验积累而提升** | 分层：上层**意图/子目标选择器** + 下层**执行器**（overcooked_ai 自带近最优 planner，零训练）。上层输入 = (状态 s, **经验上下文 E**) |
| Bob | 观察者，携带对 Alice 的"印象" | 小监督意图推断模型，训练数据 = 见过的 Alice 轨迹（秒级训练） |
| 世界 | Overcooked 布局 | 无显式通信；Bob 只能靠观测 + 自己的经验解读 Alice |

### 1.1 Alice 的"经验积累 → 能力提升"机制（本方案核心）

```
[练习循环]  Alice 连续执行多局，逐局把执行经验压缩进 经验上下文 E（跨局统计）
                每局轨迹同步离线落盘（练习轨迹本身即数据资产）

   E 增长到预设阈值时冻结快照 → 定义能力层级 L0..Lk（0 / 少量 / 中量 / 完整经验）

[评估循环]  冻结的 E_level + 下层 planner，在固定初始状态集上跑 N 条确定性轨迹
                逐 timestep 记录 + 意图标注 + 能力标签 → 离线数据集
```

**经验上下文 E 的内容（手工抽取规则，透明可审计，确定性）**——全部由轨迹日志离线/在线统计而来，跨局累积：

| 经验类型 | 统计量示例 | 对决策的作用 |
|---|---|---|
| 伙伴行为模式 | 伙伴在原料台/锅区/出餐口的驻留频率、常用路线 | 选互补站位/子目标、避免互堵 |
| 自身拥堵记忆 | 哪些走廊/柜台前发生互堵与等待 | 规划时绕行/改道 |
| 任务流节奏 | 每单平均耗时、锅位使用、停滞原因分布 | 长程意图：提前备料、预占位、按节奏取送 |

上层选择器把 E 折算成子目标优先级/前瞻深度（规则映射）。**能力层级由 E 的"量/全"决定**：E 空 → 反应式就近动作；E 全 → 前瞻性意图（如"手空但锅将完成 → 预位待取"而非"就近乱抓"）。

> 诚实边界：目前"轨迹 → E"的压缩与"E → 优先级"的映射是手工规则。**"如何自动从轨迹学出有用经验"本身是后续研究问题**；本阶段用手工版验证机制，并保证接口（§8）可被后续学习模块替换。

**经验提取与去噪原则（落地版，Alice 与 Bob 两侧同一纪律）**：
1. **目标条件化**：每条经验必须声明"它服务于哪个预测/决策目标"（如"避堵"、"互补分工"）；只保留服务于某目标的统计，其余一律视为无关信息丢弃；
2. **结局条件化对比**：同层跑 K 局并按结局打标（顺畅 / 互堵 / 慢），对比顺畅局与堵局的**情境统计差异** → 数据驱动的候选经验（可审计、非手拍）；
3. **样本量门控**：统计量需 ≥ 阈值局数才准予进 E，杜绝单局偶然进经验（防噪声第一道闸）；
4. **消融验证**：每条 E 条目做摘除测试——移除后 Alice 意图/行为是否按设计变化、表现是否变差；不生效的条目删除；
5. **更新与遗忘纪律**：新旧经验按可靠性与新旧加权，过期条目降权（与 Stale/Updated 对照共用同一机制）。

**与赛道 D（轨迹 → 可复用经验/上下文）的对应**：本方案的提取目标与 LLM 智能体"轨迹 → 技能库/记忆"路线同构——[Voyager 技能库](https://arxiv.org/abs/2305.16291)、[generative agents 记忆+反思](https://arxiv.org/abs/2304.03442)、[Reflexion](https://arxiv.org/abs/2303.11366)、情景记忆（[NEC](https://arxiv.org/abs/1706.09539)）、技能/选项发现（[DIAYN](https://arxiv.org/abs/1802.06070)）。本阶段用"结局条件化统计 + 门控"实现同一目标的可解释实例（零训练、可审计）；**学出来的提取器（小模型/世界模型）推迟到后续阶段**（§8 接口已预留），届时只替换提取第 1~2 步，输入输出不变。

### 1.2 布局与运行参数（默认值）

| 项 | 默认 | 说明 |
|---|---|---|
| 布局 | `cramped_room` | 空间窄、碰撞/等待多 → 行为歧义大；`forced_coordination` 作稳健性复核 |
| 伙伴 | **自写确定性脚本伙伴**（刻意习惯：常驻左侧洋葱台取料→锅区→出餐口），同 seed 全可复现 | 习惯固定 → E-P1 伙伴统计可学、分工分歧有素材；Alice 改善全部归因于自身经验。LLM 伙伴不进主实验：在线随机性污染经验统计且不可复现；若需"类人"对照，后续将 LLM 决策离线蒸馏成冻结策略再跑 |
| 单条评估轨迹 | 固定 300 timesteps 或固定 5 单 | 全程统一 |
| 观测 | 全观测为主；另留 ego-centric 变体字段位 | 为人类/世界模型场景铺路 |
| 种子 | 评估轨迹按 seed 确定性重放 | 层级间构成匹配对 |

---

## 2. 核心概念操作化

### 2.1 行为
行为段 = 连续 $m$ 步状态-动作片段 $(s_t, a_t, \dots, s_{t+m})$。可比性三层：状态级相同（从完全相同状态出发，最强）、前缀级相同（共享前 $m$ 步）、语义级相似（embedding 距离）。

### 2.2 意图
意图 = 行为段当前服务的子目标，离散集 $\mathcal{J}$（类型 + 目标绑定，见 §2.5）。**两类标签来源，主次分明**：

- **主标签（透明真值）**：Alice 分层可审计，其上层选择器显式持有当前意图实例 → 逐 timestep 记录 Alice 的**真实内部意图**。Bob 实验 ground truth 用此。
- **审计/回退标签（realized intent）**：行为段结束时刻向前看 $L$ 步（建议 $L=15\sim30$），窗口内第一个任务事件类型；无事件则 `IDLE`。用于交叉验证主标签、No-memory 基线、以及未来学习型（不透明）Alice 的回退。

| 意图 $j$ | 触发事件 |
|---|---|
| `FETCH(x)` | 从原料台抓起原料 $x$ |
| `PLACE(x→P)` | 原料 $x$ 放入锅 $P$ |
| `RETRIEVE(P)` | 从完成的锅取汤 |
| `DELIVER` | 汤在出餐口交付 |
| `PASS` | 递给伙伴 |
| `IDLE` | 窗口内无任务事件 |

理由：分层 Alice 使"她心里想做什么"成为显式可读变量，这是手写架构的红利——验证更强（Bob 学到的确实对应 Alice 真实子目标）；realized 回标保留，为学习型 Alice 与外部审计兜底。10% 子集用近最优 planner 最大似然归因与 realized 标签交叉校验（一致率 <85% 则回查规则/窗口）。

粒度说明：realized 回标**看不到 PRE/HOLD**（预位是内部意图，只有后续 RETRIEVE 事件才显形）→ 交叉校验只针对事件类意图（FETCH/PLACE/RETRIEVE/DELIVER/PASS）进行；PRE/HOLD 主标签以 Alice 日志为准，不参与一致率考核。

### 2.3 能力层级
$L_i$ = 经验上下文冻结于 $E_i$。**能力不等于 E 大小本身**，须由冻结评估的行为学向量佐证：

$$\mathbf{c}(L_i)=(\text{每单耗时},\ \text{交付率},\ \text{空闲率},\ \text{碰撞率},\ \text{路径次优度 vs planner},\ \text{动作熵})$$

层级有效性门槛：$\mathbf{c}$ 关键维度随 $E$ 增长显著单调改善。若不单调，调整 E 阈值分桶（按测量分层，而非等量分桶）。

测量协议：能力向量在**固定评估种子集**（独立于经验来源轨迹）上、同 seed 逐层重放测得；Alice 决策与伙伴的所有随机源均按 seed 固定，保证层级间与同层重复完全可比。

### 2.4 Bob 的"印象"（Resume 最小代理）
Bob = 意图推断模型 $f_\theta$（行为段 → 意图分布），训练数据 = 见过的 Alice 轨迹：

| 条件 | Bob 经验来源 | 含义 |
|---|---|---|
| Stale | 只用到某旧层级轨迹，冻结 | 印象停留在过去（动机受害方） |
| Updated | 旧 + 新轨迹增量训练 | Resume 持续更新的理想化 |
| Oracle | 用该层级自身轨迹训练（量对齐） | 无滞后误差上界 |
| No-memory（补） | 无跨层级记忆，在线 goal recognition | "临时协作"基线 |

**Bob 印象的具体形态（两卡片设计）**——全部内容只来自"Bob 过去看过的 A 录像"，随看到新录像而更新：

1. **水平卡（能力层）**：Bob 认为 A 目前处于哪一档（档位信念）。更新 = 把近期录像的表面统计（出汤节奏、空闲比例）与各档"签名"比对，便宜、几乎实时；
2. **经验本（行为—意图层）**：按档位分章的统计表——"情境 X + 动作序列 W → A 通常意图 J"，每章只收录该档录像的统计。预测 = 翻到水平卡所指的那一章查表。

三条件只动一张卡：Stale = 水平卡冻结旧档、新录像进不了经验本；Updated = 水平卡随证据更新 + 经验本持续增补；Oracle = 水平卡直接置真、经验本用该档录像预填（现实不可得的参考上界）。Bob 推理时不读 A 的真实意图，也不含环境规则——知识全部来自旧录像，这正是"档案会过期"的来源。该设计使三条件唯一差别 = 印象新鲜度，变量干净；分章经验本让"水平误判如何扭曲意图解读"可读可审计，且输入输出与后续 Resume/世界模型同构。

稀疏回退：新档位行为在经验本中尚无条目时 → 回退最近旧档章节并输出**低置信**（这正是"用旧印象硬猜新水平"的机制）；Updated 随新章数据增补自动填实，Stale 永不填实。No-memory 基线 = 用近最优 planner 对候选意图的在线似然匹配，无任何跨档记忆。

### 2.5 意图与动作的实例化（编码级）

**意图实例（代码对象）**：
```
Intent(kind, target, ingredient, born_t, done_t, status, cause)
kind ∈ {FETCH, PLACE, RETRIEVE, DELIVER, PASS, PRE, HOLD}
# PRE = 预位待取（长程意图）；HOLD = 无任务原地等待
# target 绑定具体对象 id（原料台/锅/出餐口）；ingredient 仅 FETCH/PLACE 用
# cause 记录触发它的 E 经验条目 → 审计：层级间选择差异可解释
```

**Alice 每步内部流（意图即数据流）**：
1. **子目标生成器**：从状态推出现阶段候选子目标（带前提，如 `RETRIEVE(P)` 需 P 完成、`PLACE(x→P)` 需手持 x）；
2. **选择器**：规则把 E 折算成优先级/前瞻 → top-1 成为当前意图实例；
3. **执行器**：overcooked planner 把意图实例化为原子动作序列（`NORTH/SOUTH/EAST/WEST/STAY/INTERACT`）逐步执行；
4. 每 timestep 落盘 `intent_id` + 原子动作 + 状态 → 意图、动作、状态一一可追溯。

**Bob 侧的实例化**：Bob 只见行为段（观测 + 动作窗口），输出 $P(J \mid X)$；训练标签取 Alice 日志真值意图。**"相同行为、不同意图"在数据里的样子**：两个匹配的行为段（相同状态/动作前缀）挂不同真值意图标签——如同为"沿走廊北上"前缀：$L_0$ 标 `FETCH(onion@台左)`、$L_k$ 标 `PRE(POT_A)`（预位即将完成的锅），第 $m+1$ 步才分流；probe 采样即专门收集此类前缀。

---

## 3. 数据管线（数据资产核心，schema 同 v1）

练习与评估轨迹**全部落盘**。逐 timestep 字段组：元数据（`session_id, level_id, seed, layout, partner_id`）、真值状态（双 agent 位姿/手持、锅内物与进度、台面物件、订单状态、全局时间）、各自 featurized observation、动作、意图标签 + 触发事件 id、事件流底稿（(t, 类型, 对象)）、回报、运行期统计。每层级另存 **E 快照**（memory 版本号），保证未来精确重放。

- 格式：`.npz`/parquet 分卷（每 (level, seed) 一卷）
- 统一 loader：按 (layout, level, seed) 切分；全部实验脚本只读 loader
- **接口约定：schema 即后续世界模型训练输入契约；新增字段向后兼容追加，不重排**

## 4. "相同行为"采样与配对（同 v1）

- **(a) Probe 受控对照（实验一主证据）**：抽"高歧义状态"集（≥2 候选意图合理，planner 似然比筛选 + 人工抽检）；**每层级从同一 $s$ 续跑** → 成对比较意图分布。
- **(b) 自然公共前缀配对（补充）**：同 seed 下 $L_i$、$L_j$ 同步重放，首个分歧动作前的公共段即"相同行为"。

翻转概率（成对估计）：$\text{flip}(x; i,j) = P(J^{(i)} \ne J^{(j)} \mid x)$。区分两种成因：
1. **重解释**：同一行为前缀、目标不同（如走向某处：低级=就近乱抓，高级=预位待取）→ 动机**核心证据**；
2. **novelty**：升级后出现全新状态/行为模式 → 次级滞后源。

实验二分开报告二者，避免混淆。

---

## 5. 实验设计

### 5.1 实验一（P1）：同一行为，意图是否随能力而变
- 材料：$k+1$ 层级 × probe 状态（成对续跑，数百~上千）+ 自然公共前缀对（数千）
- 主度量：平均翻转概率 $\overline{\text{flip}}$（bootstrap CI）；按意图分组的迁移混淆矩阵；$\overline{\text{flip}}(L_0,L_{mid})$ vs $(L_0,L_k)$ 层差趋势
- 统计：McNemar（同状态配对）+ bootstrap
- **G1 门槛（先行小试点）**：$\overline{\text{flip}}(L_0,L_k)$ CI 下界 > 0，且重解释类翻转占比可观（>20%）。不过 → 迭代 E 的统计项/前瞻规则（便宜，非训练）；仍失败 → 换布局或诚实上报动机不成立

### 5.2 实验二（P2）：陈旧印象的伤害与更新恢复
- Bob 经验截止 $L_i$，测试于 $L_{i+1}$ 及其后；四条件如上
- 度量：翻转子集准确率（Stale vs Oracle，预期显著为负）；全量子集（novelty 机制）；混淆矩阵（陈旧印象把新意图错标成什么——论文叙事图）
- 主图：横轴 session/层级推进，纵轴推断准确率，Stale / Updated / Oracle 三线。预期：升级点 Stale 掉且不恢复；Updated 短暂下探后回升；Oracle 平直
- **G2 门槛**：升级后 Stale 相对 Oracle 显著下降；Updated 在有限新经验内（≤ 数百条或 ≤ session 数据 1/4）回升至 Oracle 的 90% 区间

执行细节：对**每个相邻升级** $(L_i \to L_{i+1})$ 各跑一遍四条件对比——每个升级点是一次独立效应复制，报告跨升级点的一致性、均值 ± CI。Stale 的"升级前基线"在其训练档自身的测试集上先测（H2b 对照）。

### 5.3 Bob 决策质量（次要验证）
Bob 用手写响应策略 $g(\hat j)$ 变成可行动搭档（依推断选互补子目标，不加训练，干净传导"推断→协作"）。度量：团队奖励、协助对齐率、碰撞/互堵占比。预期图形与实验二同构。

### 5.4 稳健性
层级稳健性（多随机种子下 Alice 行为的可复现性、以及 E 分桶边界 ±1 档的稳定性）；布局复核（`forced_coordination`）；样本自适应（pilot 定效应量后固定预算序贯采集）。

---

## 6. 指标汇总

| # | 指标 | 用于 |
|---|---|---|
| M1 | probe 平均翻转概率 $\overline{\text{flip}}$ + CI | P1（G1） |
| M2 | 翻转混淆矩阵 | P1 叙事 |
| M3 | 能力向量 $\mathbf{c}(L_i)$ 随 E 单调性 | 层级有效性 |
| M4 | 意图推断准确率（四条件） | P2（G2） |
| M5 | Updated 恢复曲线（达 Oracle-90% 所需新经验） | P2 |
| M6 | 标签交叉校验一致率 | 标注质量 |
| M7 | 团队奖励/协助对齐率/碰撞占比 | 决策质量（次要） |

## 7. 假设（预注册风格）
- **H1**：probe 上成对续跑意图翻转概率显著 > 0，且随层级差增大。
- **H2**：升级后 Stale 在重解释翻转子集准确率显著低于 Oracle（a）且低于其升级前水平（b）。
- **H3**：Updated 摄入有限新经验后回升至 Oracle 90% 区间；Stale 不恢复。
- **H4（次要）**：可行动 Bob 变体中团队奖励/协助对齐随条件同构变化。

---

## 8. 与后续"世界模型反推意图"阶段衔接（同 v1，数据契约）

1. per-timestep **主标签（Alice 显式意图序列）** = 未来世界模型/意图推断器的监督信号；realized 事件回标为不透明智能体的回退；事件流底稿支持改标签规则不重跑环境
2. 每条轨迹带 `level_id` + 能力向量 + E 快照版本 → "能力条件化"意图模型（Resume 能力层雏形）
3. schema 预留部分观测/ego-centric 字段位
4. 统一 loader，杜绝旁路硬编码

## 9. 里程碑与风险（零训练后大幅变轻）

| # | 内容 | 交付物 | 估算 |
|---|---|---|---|
| M0 ✅ | 环境落地：`overcooked-ai` 安装 + 布局/菜谱/planner 核对（详见文末 M0 核验记录） | smoke test 脚本 `scripts/m0_env_check.py` | 已完成 |
| M1 ✅ | Alice 实现：E 统计项 + 上层规则映射 + 层级冻结；能力向量验证（详见文末 M1 记录） | 能力曲线 | 已完成 |
| M2 | 轨迹收集 + 事件流 + 意图标注 + loader | `data/level_*` + 一致率 | 1 天 |
| M3 | 实验一（probe + 公共前缀；**开头先跑 G1 小试点半天，通过才铺全量**） | 翻转统计 + G1 判定 | 1~2 天 |
| M4 | 实验二（四条件 + 恢复曲线） | 主图 + G2 判定 | 2~3 天 |
| M5 | 可行动 Bob 变体 + 验证报告 | 决策质量图 + 报告 | 1~2 天 |

总量级：一周内可完成验证（视 rollout 数据量），无 RL 调参不确定性。

| # | 风险 | 对策 |
|---|---|---|
| R-context | E 统计项选得不好 → 层级间行为无差异 | 迭代统计项/前瞻规则（便宜）；能力向量把关 |
| R2 | G1 不过：无意图翻转 | 小试点先行；扩大歧义状态集；换布局；诚实上报 |
| R3 | novelty 与重解释混淆 | 按子集分开报告；公共前缀配对隔离 |
| R4 | Updated vs Oracle 不公平 | 旧数据与 Stale 一致，仅新增新数据；训练量对齐 |
| R5 | 标签与真意图偏差 | planner 交叉校验；事件流底稿可重标注 |
| R6 | Bob 过拟合布局/状态分布 | probe 与自然子集分开报告 |

## 10. 交付物
1. 环境 + 数据管线代码（Alice 经验循环、收集、标注、loader）
2. `data/level_*` 分层轨迹数据集（含 E 快照、意图标签）
3. 能力向量曲线与层级有效性报告
4. 实验一统计（H1 + 混淆矩阵）
5. 实验二主图与统计（H2/H3 + 恢复曲线）
6. 决策质量变体（H4，次要）
7. 动机验证报告（G1/G2 判定）

## 附：本阶段明确不做
- 不训练 RL；不构建完整 Resume（经验统计层/能力后验层/行为模式层）；不训练世界模型/逆 RL——数据契约为其铺路
- 不做在线连续演化/双方共同演化/人类被试；Bob 可行动变体用手写响应策略

---

## M0 环境核验记录（已完成）

- 环境：`.venv`（Python 3.11.3，Windows）；`overcooked-ai==1.1.0`；`numpy==1.26.4`（必须 <2，包内用 `np.Inf`）；`scipy>=1.11`（env/planner 未声明依赖）；见 `requirements.txt`
- 布局 `cramped_room`（5×4，洋葱汤 ×3）：双洋葱台 O、单锅 P、盘 D、出餐 S、玩家位 1/2；初始状态确定性
- 动作空间 6：移动以 `(dx,dy)` 元组表示 + `'interact'`（含 `(0,0)`=STAY）
- 执行器地基现成：`OvercookedEnv.mlam`（MediumLevelActionManager）+ `env.mp`（MotionPlanner），首跑自动缓存 planner pickle 到 site-packages
- 观测/特征：`env.featurize_state_mdp(state)` → 每 agent 96 维向量（供 Bob 推断输入）
- 轨迹 API：`env.run_agents(pair)` 返回 `(轨迹数组, timestep, sparse_reward, shaped_reward)`；轨迹行为 `(state, joint_action, r, done, info)` 元组
- **确定性已验证**：自写 CycleAgent 双跑 60 步逐状态/动作完全一致；`env.step(joint_action)` 手动控制可用
- 行为学向量素材：`env.proportion_stuck_time(trajectories, agent_idx)` 为事后计算接口（M2 用）
- 包已知缺陷：`RandomAgent` 在 numpy 1.26 下坏（`np.random.choice` 混合类型）→ 本方案本就自写确定性 agent，绕开
- 遗留警告：legacy gym 的 NumPy2/迁移提示——不影响本方案（不依赖 gym.Env 包装）
- 遗留到 M1：正收益的完整做菜循环（取洋葱→下锅→等熟→端汤→交付）尚未跑通——这是 M1 Alice 执行器的验收点

## M1 前置规格：E（经验上下文）设计（编码前敲定稿）

目标：让 E 的**量变**产生**可测的意图分歧**，且每条 E 都服务于明确决策、可开关（消融用）、可审计。约定 Alice 为"取洋葱×3 → 下锅 → 等熟 → 取汤 → 出餐口交付"的洋葱汤循环（cramped_room 无盘对象，交付不需要盘子——M1 以代码事件流复核）。伙伴为确定性脚本（§1.2），其固定习惯是 E-P1 可学性的前提。

### E 目录（统计项 → 服务决策）
| id | 统计量（跨局累积） | 服务决策 |
|---|---|---|
| E-P1 | 伙伴在 左洋葱台/右洋葱台/锅区/出餐区 的驻留频率（近 W 局滑动） | 分工互补：伙伴常驻左台+锅区 → 我取对侧料或改担待锅角色 |
| E-C1 | 各走廊格/路口 双方静止 ≥k 步的互堵事件频率 | 路线再选：穿越常堵点前查伙伴位置 |
| E-C2 | 最近互堵/死锁点 top-k 位置 | 绕行与避让优先级 |
| E-T1 | 每锅"下料→完成"实测用时均值±sd | 前瞻触发：锅进度预测 |
| E-T2 | 每单耗时、单局交付数、空闲占比分布 | 忙闲预算：空闲期该预位还是该备料 |
| E-T3 | 顺畅局 vs 停滞局的任务序对比（结局条件化） | 次序规则：锅 ≥2/3 且无人看管 → 看锅优先于再抓料 |
| E-S1 | 自身单局结局标、碰撞次数（轻权重） | 审计/后续 Resume 层用，不主导决策 |

### 决策规则（selector 里 E 条件分支，全部可单独开关）
每条：`if 条件C(状态+E统计) then 子目标g 优先级 += Δ，权重=置信w`

- R1（分工）：手空 且 伙伴驻留锅区+一侧台高频 → 优先对侧 FETCH 或 PRE（看锅）
- R2（避堵）：计划路径穿越 常堵点（E-C1/C2）且伙伴邻近 → 改选路线/目标
- R3（前瞻预位）：锅完成概率 > θ(E) 且手空 → PRE（锅旁预位待取），压过就近 FETCH
- R4（次序）：锅 ≥2/3 无人看管 → 看锅（RETRIEVE 预备）优先于继续囤料
- R5（忙闲）：无紧迫任务且交付节奏落后预算（E-T2）→ 主动承担下一单取料/看锅，避免两人同闲或同抢一锅

**意图分歧的来源**：L0 只有基础就近启发（反应式）；L1 起逐档解锁 R3/R4（前瞻）、R1/R2（伙伴/避堵）——同一状态与移动前缀下，E 满档选"预位/看锅/互补"，E 空档选"就近抓料/乱走"。

### 层级分桶（练习局数，初值待 pilot 校准）
L0=0 局；L1=30；L2=120；Lk=400（伙伴固定基线不变）。**能力有效性门槛**：交付率/每单耗时/互堵占比随桶单调改善（§2.3 测量协议），否则调桶或调规则权重。

### 门控与审计（防噪声，原则同 §1.1）
- 统计需 ≥10 局样本；规则需 support ≥10 次且 顺畅/停滞局比例差 ≥0.15 才转正
- 规则置信 w 随局龄衰减（τ≈50 局）；层级冻结时衰减停用（保留机制给 Updated）
- **审计字段**：每条 top-1 意图记录 winner 规则 id；凡 E 规则改变 L0 基线选择 → 打 `E_influenced` 标记（供 probe 采样与消融统计）

### M1 验收（承接 M0 遗留）
1. 正收益完整做菜循环跑通（交付 >0）
2. 逐档能力向量单调改善
3. 抽 50 个歧义状态做 50×50 步同起点续跑试点：出现 ≥1 类重解释翻转（G1 迷你版）

## M1 实现记录（已完成）

代码（`src/ocres/` + `scripts/m1_*.py`）：
- `grid.py`：几何/寻路/交互站位（坐标 (x=列,y=行)）
- `executor.py`：确定性微执行器——走向+朝向+interact 动作解析（含受阻等待）
- `recipes.py`：锅状态/持有物/烹饪语义 helper
- `agents.py`：`CookAgent`——显式意图状态机（FETCH/PLACE/COOK_START/GET_DISH/PICKUP/DELIVER/PRE/HOLD），按交付奇偶轮换 COOK/SERVE 互补角色（单锅避免互抢），E 旋钮 = `fetch_ahead_delay`（烹饪期内预取下一洋葱的时延），含非对称退让破死锁、角色切换库存兜底
- `runner.py`：确定性 rollout + 逐 tick 日志（位置/意图/持有/锅态/动作/奖励）+ 能力度量

机制核对（teleport 探针）确认：下 3 洋葱 → 空手 interact 锅 = 显式开始烹饪（20 tick）→ 需先持盘才能取汤 → 出餐口 interact 交付 +20。

**M1 验收结果**（cramped_room 单锅，600 步，同 seed 确定性）：
1. ✅ 正收益循环：基线 12 碗/600 步（400 步 8 碗），奖励 >0，无死锁
2. ✅ 能力单调：E 累积推导链 L0(无经验)→L1(测得间隔 48.6→学得 delay=8)→L2(42.2→delay=0)→Lk：交付 12→14→14→14，平均间隔 48.6→42.2→42.2→42.2
3. ✅ 迷你翻转试点：L0 vs Lk 找到 1 例重解释翻转（t=21 烹饪中：L0 空等 HOLD vs Lk 预取 FETCH@0,1——R3 类）

**诚实记录的限制**（驱动 M2 决策）：
- 能力在 L1 即饱和：单锅串行瓶颈下"预取一颗洋葱"是几乎唯一的前瞻杠杆，delay 细粒度无差别
- 翻转仅 1 例：意图空间由单锅/单菜谱决定，重解释场景少
- 对策（M2/M3 选项）：①转多锅布局（counter_circuit/forced_coordination）扩展意图空间与 E 杠杆；②在单锅上只能接受弱效应——不推荐
- 训练管线无 RL；E 推导规则现为"结局条件化 + 阈值"（§1.1 原则），统计累积在 M2 形式化为数据资产

## M1-双锅 扩展记录（换布局重跑，已完成）

forced_coordination 两半不连通（x=2 全墙）不可用；counter_circuit 订单含无源番茄不可用 → **自建连通双锅布局**（`World.make(grid_rows=...)`，"XXPXPXX/O     O/X 1 2 X/X D S X/XXXXXXX"：双锅、双侧洋葱、单盘、单出餐、中央顶墙产生绕行前缀）。
多锅泛化修复（`agents.py`/`executor.py`）：
1. 锅目标选择（多锅：取接受位最高的锅、ready 就近取汤）+ 角色切换库存兜底
2. **executor bug**：被占的"接近格"被 BFS 豁免 → 反复撞格不绕行 → 占用即等待
3. **停靠振荡 bug**：HOLD 每 tick 重算最近自由格 → 两格间死循环 → 进入 HOLD 锁定停靠格、到位即停

验收（900 步同 seed 确定性，伙伴固定串行中档）：
- L0（串行，一锅煮完才开下一锅）：交付 20，间隔 44.0
- Lk（并行，锅在煮时补另一锅）：交付 **28**，间隔 **31.0**（能力提升显著且单调）
- 迷你翻转：≥1 例重解释（t=20 同状态 L0=HOLD 空等 vs Lk=FETCH 补锅）
- 无死锁（最长停留 ~20）

说明：能力杠杆从"单锅预取"升级为"双锅并行度"（强、可调）；意图空间扩展出 PLACE@锅A/B、PICKUP@锅A/B 维度。正式 E 统计累积与门控在 M2 随数据资产实现。

## M2/M3/M4 执行记录（数据集 + 翻转统计 + Bob 实验，已完成）

**M2 数据资产**（`src/ocres/data.py` + `scripts/m2_generate.py` → `data/twopot_v1/`）：
- 每 (层级 × seed) 一卷 npz：逐 tick 全量字段 + meta.json（metrics/game_stats/延迟旋钮）；统一 loader
- 事件流从 env.game_stats 提取（onion_pickup/potting_onion/dish_pickup/soup_pickup/soup_delivery → FETCH/PLACE/GET_DISH/PICKUP/DELIVER）
- realized 审计标注（向前看 L=20）：事件类 tick 主标签-回标一致率 **~0.99**（标注质量达标）
- E 统计：`summarize`（能力向量 + Alice 意图分布 + 伙伴驻留 E-P1 素材）+ 结局条件化推导规则（大间隔→更早并行）

**层级能力（900 步 × 3 seeds 均值）**：L0（串行）20 碗/间隔 44.0 → L1(延迟12) 20.7/42.4 → L2(延迟6) 20.3/42.4 → Lk（并行0）**28 碗/31.0**（Lk 显著提升；L1/L2 微升，报告如实含噪）

**M3 翻转（正式 G1）**：L0 vs Lk 匹配前缀重解释翻转 **3/3 seed 各 1 例**（每局首个"锅在煮→是否补另一锅"决策窗口：L0=HOLD vs Lk=FETCH），同上下文异意图集合一致

**M4 Bob 实验**（`src/ocres/bob.py` + `m4_bob_experiment.py`，leave-one-seed-out，Lk 整局测试）：
- 总准确率：Stale **0.880** < Updated **0.968** < Oracle **1.000**（印象新鲜度排序正确）
- **翻转子集**（同上下文、真意图不同）：Stale 0/3、Updated 0/3、Oracle 3/3 —— 陈旧/未更新印象在这类点上系统性误读，符合动机预测
- 门槛判定：G1 通过（翻转存在）；G2 方向成立但翻转子集样本量仅 3 → 需 probe 法扩样（M3 正式版）后做统计检验

**诚实限制**：同 seed 同步重放下两策略首个分歧后轨迹去同步，整局"同上下文异意图"实例每局仅 1；扩大翻转样本的正路 = §4(a) probe 受控续跑（多状态 × 多层级成对续跑），列入下一轮。

## M5 实弹 LLM pilot 记录（Qwen3.5-9B / 硅基流动 / 混合式，已完成首轮）

架构：Alice/Bob 均为 LLM 混合体——脚本 FSM 逐 tick 兜底 + LLM 仅在决策点选子目标；角色固定（Alice=cook，Bob=serve）；Alice 经验/层级=硬规则卡（Lk: 锅在煮且另一锅可补料且空手 → 必须 FETCH），Bob 印象卡分 stale(以为串行)/current(知道并行)；Bob 每决策输出 `alice_guess`，打分窗=Bob 决策纪元内窗口匹配 Alice JSON 自报意图。
关键修 bug：Bob 猜测曾用 serve 合法池（无 FETCH）→ 拆 Alice 视角池后才可能猜对并行；猜测词规范化（COOK→COOK_START 等）。

结果（2 局/条件，窗口准确率，div=并行补料 tick 上 Bob 猜 FETCH 命中）：
- **current 卡：总准确率 92.3%/90.0%，div 3/4**
- **stale 卡：总准确率 55.2%/65.8%，div 0/11**
- 交付 2–4 碗/260 tick；每局 ~75–135s、调用 ~60–100 次

结论（pilot 级，方向明确）：印象与 Alice 真实水平一致时 Bob 能读对升级后行为；印象停留在过去时在"同状态不同意图"点上系统性误读（0/11 vs 3/4）。

**诚实限制**：n 小；Alice 实际并行程度逐局波动（LLM 随机性）；条件间 Alice 行为未完全匹配；Updated(在线刷新)卡与统计检验未做——均列入下一轮。

## Motivation 验证收口（确定性管线统计版，已完成）

Probe 经验：跨局独立轨迹做**精确上下文**配对仅得 1 例（位置已分叉）；样本扩大靠同 seed 整局同步对比 + 多 seed 重复（确定性 → 每 seed 为同一试验的复制，无组内方差，报告时注明）。

**8-seed 整局留一（horizon 700，Bob 统计两卡）**：
- 能力：L0 15碗/44.0 → Lk 21–22碗/30.9（经验推导的并行规则生效）
- Bob 总准确率（对 Lk 行为）：**Stale 0.877 < Updated 0.968 < Oracle 1.000**
- 重解释子集（同上下文、真意图不同）：**Stale 0/8、Updated 0/8、Oracle 8/8**
- 附加：LLM 实弹三卡同向（current 79.9% > stale 62.4%；分歧点 3/6 vs 0/7）

**结论（受控证据，机制级）**：①能力确实随经验推导规则提升；②升级后存在"同一可观测状态 ↔ 不同真实意图"的重解释点；③印象停留在过去的 Bob 在这些点系统性误读（0/8），印象随证据刷新可恢复（0.968），知情的 Oracle 全对（1.000）。Motivation 的机制命题在确定性管线中完成闭环验证。
诚实边界：确定性环境无组内方差（seed 为同试验复制）；脚本规则为经验提升的受控代理；LLM 端为方向性展示（n 小、噪声大）。

## 评审记录（v3 审核）

**总体结论：设计自洽、范围可控、无阻塞性缺陷，可以进入 M0 环境搭建。**

本次审核发现并已修订：
1. 架构代号"D"与赛道 D 重名混淆 → 标题/§1.1 改为"经验即上下文 = 赛道 D 的可解释实例"
2. 经验提取路线未点明文献出处 → §1.1 对齐赛道 D 并挂起点文献（Voyager / generative agents / Reflexion / NEC / DIAYN）
3. realized 审计标签看不到 PRE/HOLD → §2.2 限定交叉校验范围为事件类意图，避免误判标签质量
4. 能力向量测量协议缺固定种子集 → §2.3 补"固定评估种子集、同 seed 逐层重放、随机源全固定"
5. Bob 经验本对新档无条目时的行为未定义 → §2.4 补稀疏回退（旧章 + 低置信）机制
6. 实验二仅单次升级、效应无复制 → §5.2 改为每个相邻升级各跑一遍
7. §8 世界模型监督信号仍写 realized → 改为主标签（显式意图），realized 降为回退
8. G1 试点时机不明 → M3 开头半天小试点，通过才铺全量

仍留待 M0/M1 现场确认（不阻塞搭环境，以实际源码为准）：
- `cramped_room` 实际菜谱/物件/planner 接口；gym/gymnasium 兼容 shim
- 每档 E 阈值（练习局数）、结局打标局数 K、样本门控阈值具体数值（pilot 定）
- probe 歧义状态集规模（数百~上千，按实际可采样密度定）
- 伙伴基线具体形式（planner 中档或脚本）
