# M18 进度

## 2026-09-15：用户批准与本地验收

用户已同意执行方案中的五项选择，正式方向为 3 个 Alice 训练 seed × 3 张地图 × 50 个配对事件，预测全部 8 类意图；额外 D 普通事件和 B 完整合作局也已同意。预算按方案执行，记录实际 usage。

已完成：

- 参数落盘至 `configs/m18_v1.json`，包括 seed 范围、训练预算、评估量、门槛和 API 上限。
- 新增 M18 专用训练代码和入口；交替角色示范、角色对应等待位置、BC、PPO、按验证选 checkpoint、独立测试门槛、哈希和覆盖保护。
- 新代码使用每份汤 20 分计数；若同 tick 两份汤交付计为 2，不沿用历史 runner 中每个正奖励 tick 只算 1 的简化。
- 5 项针对性测试通过：来源 seed 隔离、三张地图合法起点和连通性、等待角色切换、双交付计数、任务事件不按 tick 重复。
- 实际环境核对：Conda agentresume，Python 3.11.16，PyTorch 2.1.0+cu118，NumPy 1.26.4，CUDA 可用，NVIDIA GeForce MX450。

修复前先导运行 `v1_20260915` 中发现：C 地图在一条轨迹上，Alice 的 PRE 等待目标每 tick 被重选，与持汤出餐的 Bob 在两格间循环，最后一次交付为 tick 383。旧的训练组 0 结果只供开发诊断，不进入 M18 正式样本。

M18 专用执行代码增加目标等待稳定性与两格往返检测；C 地图同一起点重新回放后，最后 20 tick 不再只有两种位置组合，tick 400 以后继续交付。M18 12 项针对性测试通过。修复版运行 ID 为 `v1_waitfix_20260915`，保存当时的配置和源文件哈希及源码副本。

修复版地图预检：三图示范平均交付分别为 A 14.00、B 14.375、C 15.625 份汤/700 tick；三图八类示范均非零。训练组 0（地图 A、训练 seed 1801）已通过独立能力测试：pre=14.00、post=20.00 份汤，同起点平均 +6.00，32/32 起点提高。post 由验证集选在 update 50，不根据 Bob 结果选择。结果见 `data/m18/v1_waitfix_20260915/group_0/result.json`。

Bob 的本地可见观察、8 类 JSON 校验、三票投票和公共信息控制器已有独立实现与测试。组 0 的历史印象验收通过：旧/新各 76 段连续可见历史，来自 38 个配对起点，见 `data/m18/v1_waitfix_20260915/group_0/history/impressions.json`。正在采集来源轨迹与配对事件，尚未调用 Qwen；如果按预先约定的严格规则不足 50 个，保留实际数量和排除原因，再修订方案。

组0自然来源的配对事件门槛未通过：开发来源原始11个、正式来源原始80个；正式80个中78个公开事件哈希已在开发出现，按原隔离规则正式选出0个。具体审计见 `M18_PAIR_SAMPLING_REVISION_REVIEW.md`。

用户已同意 A 改用受控局面，而 D/B 保留自然来源。组0受控 A 从648个合法可见快照得到159个严格候选；隔离历史、开发和近重复后剩75族，冻结10个开发与50个正式事件，门槛通过。正式事件的成长后意图为 FETCH 49 个、GET_DISH 1 个；A 不声称八类均得到严格配对验证。受控样本不是自然合作局。受控分割与公开/真值分文件见 `data/m18/v1_waitfix_20260915/group_0/controlled_A/`。其余训练组仍顺序运行，随后逐组核对能力、历史和受控容量。

M18 公共信息提示构造、合法 JSON 校验、三票和 API 请求账本已实现；18 项 M18 测试通过。组0开发 10 事件×3 条件×3 独立请求已开始，正式50事件尚未调用。组1历史采集与训练并行时，Overcooked 环境的共享规划器 pickle 缓存发生截断读取；已生成的32个 pre 来源局保留，历史完成前不将其视为通过。新增只补缺失来源的恢复入口，之后游戏模拟改为串行，避免再次竞争共享缓存。

组0开发提示词 v1 的90个独立预测已完成（实际92次网络尝试，含2次传输补偿）；合法格式较稳定，但三条件的三票意图准确均为0/10，几乎全部回答 unknown。因此不将 v1 用于正式推断，保留其 prompts、原始响应和得分。开发提示词 v2 仅把“证据不足即 unknown”改成“根据公开线索推最可能意图，用低信心表达不确定；绝不能编造不可见事实”，不更换任务定义、历史或真值，也不加入倾向某标签的示范。v2 仍只在组0开发10事件验收；正式样本未打开。提示词修改的门槛是有效猜测/弃答与事实引用，而不是看哪个版本刚好猜中真值。

为分辨“严格歧义本来缺少线索”和“提示词一概弃答”，从组0已有的独立自然开发轨迹抽出10个与历史及受控候选隔离的可见新任务事件作 D 开发诊断；这10个事件覆盖全部八类，每类实际数量见 `data/m18/v1_waitfix_20260915/group_0/development_D/manifest.json`。它们只校准弃答/事实引用，不纳入 A/D 正式效果统计；正式 D 仍按独立来源的实际分布均匀抽取，不能借开发数据补齐稀缺类。

组0开发提示词 v2 完成：受控 A 的旧/新/无三条件仍各0/10；三票弃答分别为7/10、8/10、10/10。自然 D 开发的旧/新/无分别2/10、2/10、1/10；弃答4/10、5/10、6/10。自然事件能让模型猜出具体类别，但仍显著偏向某些类别且准确率低。上述20个开发事件不能当作动机的正式效果；提示词 v2 根据公共事实/弃答行为而不是正确答案选择，现冻结进入后续正式测试，不再按正式结果调参。解释边界：若印象没有作用，可能是可见线索和未标注意图的历史不足，不可自动断言“伙伴成长不存在影响”。

已准备好本地 A-score 受控续局、D 独立自然事件、B 自然触发与完整合作入口；它们尚未生成正式结果。考虑共享规划器缓存的并行读写风险，训练结束后使用串行本地阶段入口 `scripts/m18_posttrain.py`，先确认九组 Alice 全部能力门槛，再逐组完成历史、受控 A 容量、D 50事件及 B 开发触发门槛。任何约定门槛失败都会停止正式 Qwen 阶段并留下原因；不通过替换 seed 或放宽去重偷偷补齐。

追加只读 D 容量审计：组0另一分区100局自然轨迹排除历史和受控 A 后，只有28个近重复族；原 D 内“一族一个”与50/组目标不相容。推荐修订为 D 100个预定自然来源局、完全相同公开事件仍去重、D 内不同细节的同族事件可保留、跨历史/A 仍隔离，细节见 `M18_D_SAMPLING_REVISION_REVIEW.md`。本变更尚待用户审核；D 正式来源与 Qwen 暂停，不把探针当正式结果。Alice 训练、已批准的 A 来源和 B 开发门槛不受影响。

2026-09-16：用户已批准上述 D 修订；参数单独落盘于 `configs/m18_d_v2.json`，不改 M18 Alice 训练源码/配置的冻结哈希。昨晚顺序训练在组5的 PPO 第44/50次更新后遭会话中断：组0–4已经通过，组5没有完成成长后 checkpoint 或独立测试，旧进度保留。为避免把中断误写成通过，先运行独立的组6–8；组5的恢复方式待单独核对，不能静默覆盖旧记录。D 的100个来源只在对应能力、历史和 A 来源门槛通过后开放。

正式提示词复核发现静态地图的 `1`/`2` 只是可通行地板标记，而原开发提示原样提供，可能被 Qwen 错当固定人物站位，与随机合法起点矛盾。修订版 `v2_mapfix` 将这两个字符在**Qwen 的静态地图文字中**替换为空地；当下人物坐标仍只来自可见 C1/C3，地图设施 ID 直接由静态字符扫描，与原开发设施表逐项一致。修改不是依据正确答案调参，历史、八类定义和严格配对真值未变。旧 v2 开发原始记录保留；组0 A/D 开发事件将用 mapfix 重新验收，之后才可冻结正式输入。设施扫描不再构造游戏世界，因此训练与 Qwen 提问不会竞争共享规划器缓存。

组0 `v2_mapfix` 开发复测已完成。受控 A 的旧/新/无仍各0/10，弃答8/10、9/10、10/10；自然 D 的旧/新/无为2/10、2/10、2/10，弃答3/10、4/10、7/10。地图标记错误已修复但未制造动机效应；这些小样本仅作输入和弃答诊断。mapfix 的提示文本、静态设施表、请求和响应已分别冻结，后续正式阶段不能再依据成绩调整。B 的在线 Qwen 提问用相同 mapfix 版本，断点续跑模拟测试验证同一触发不会重复发出已完成请求。

2026-09-16 进度补记：九组中组0、1、2、3、4、6、7、8 的 Alice 已完成 PPO 与独立能力测试，全部通过成长门槛；各组历史印象与受控 A 来源门槛也通过，受控 A 每组冻结 10 个开发、50 个正式严格配对事件。组5原 PPO 在44/50中断，旧 `training_progress.json` 和成长前 `pre.pt` 不覆盖。用户另行同意从冻结 pre 完整重跑 50 次 PPO；新续训记录放在 `group_5/ppo_restart_20260916/`，每次更新保存可恢复的网络、优化器与随机状态，明确不是44次的精确接续，能力测试通过前组5仍不能计入正式结果。

组0受控 A 的 `v2_mapfix` 开发续局：旧/新/无印象各10例，平均每例4.4份汤；给控制器真实意图/设施的 Oracle 平均5.0份汤，但7/10局面四条件得分相同，另有1例 Oracle 更差。另用组0成长后 Alice 的16个独立随机合法开发起点做 B 控制器诊断，前300步 Oracle 相比不提供预测平均**少0.25份汤**，2局更好、8局相同、6局更差；13局产生公开提问。该诊断不调用 Qwen、不使用正式 B 起点。按已审核方案，当前控制器不满足自然合作的 Oracle 效果门槛；应先分析并修订控制器，再冻结正式 B 团队得分，不将控制器失效解释为 Motivation 成立或失败。

D 的已批准100个固定自然来源局在完成的8组上全部采集并按规则验收，结果为：组0/1/2/6/7/8 各得到50个不同公开事件；B 地图的组3只得到38个、组4只得到27个，尽管各组都跑完预定100局。组3、4的跨历史/A隔离后候选公开哈希分别只有39、28个，不能通过改变 D 内近重复处理补足50。组0的50个事件来自42局、25个近重复族，八类意图均实际出现；其他组的类别覆盖不一，逐类以各组 manifest 为准。来源入口按门槛报错停止正式 Qwen；这些失败数据保留，不补 seed、不改变配额、不将已通过组冒充九组全部通过。若要继续完整450例 D，需要对 B 地图容量另提审核方案。

组5经用户明确批准后从冻结 `pre.pt` 做了**新的**50次 PPO 续训，旧44次 PPO 进度文件仍保留；新续训的每次更新状态在 `group_5/ppo_restart_20260916/`。验证选择第50次更新，独立32起点能力验收全部改善，成长前后平均差约+4.90625份汤，组5成长门槛通过。九组 Alice 的能力验收至此齐全；正在仅为组5顺序补采历史、受控 A 与固定100局 D。D 的容量后续建议另写在 `M18_D_CAPACITY_FOLLOWUP_REVIEW.md`，**待用户审核**；没有批准前正式 D/Qwen 仍暂停。

组5历史印象与受控 A 门槛已通过；其 D 固定100局只得到27个合格且不同的公开事件，原50例门槛失败。至此九组 D-v2 自然来源全部采完：A/C 地图六组各50，B 地图组3/4/5分别38/27/27，合计392，不是450。D 后续修订仍待用户审核；旧失败 manifest 不覆盖，正式 Qwen 不启动。

用户随后同意继续推荐方案。D-v3 已冻结为“每组最多50，固定100局不足时保留实际数量”，运行级接受凭据核对九组共392个事件通过；D-v2 原 manifest 和失败标记不改写。配置及接受层见 `configs/m18_d_v3.json`、`data/m18/v1_waitfix_20260915/D_v3_acceptance.json`，正式 D 入口现在同时校验 v2 原始哈希和 v3 接受凭据。

Bob 自然合作控制器在开发集经历了完整且留痕的修正。v1（广泛回避意图/设施）组0前300步 Oracle−无预测为−0.25份汤；v2（只按设施平级避让）为−0.375；v3（Alice可见且明显更近才避让）为−0.75，并有两局灾难性掉分；v4（只避让取料设施）为−1.1875。失败版本及各局结果均保留，没有用于正式实验。v5 不再把预测理解为“远离 Alice”，而只做流水线分工：Alice 开火时 Bob 在有条件时准备盘子；Alice 取盘、盛汤、送餐或等待锅时 Bob 补原料；Bob 手里已有物品时不被预测打断；目标设施不直接控制路线。

v5 规则先在组0开发后固定逻辑，再原样扩到九组共144个开发起点：主指标前300步 Oracle 平均比无预测多0.1667份汤，20局更好、117局相同、7局更差；9组中5组正向、3组持平、1组负向。700步辅助结果平均+0.3958份汤。九组公开触发门槛均通过。v5 已冻结在 `configs/m18_b_v5.json`，接受凭据为 `data/m18/v1_waitfix_20260915/B_v5_acceptance.json`；正式 B 和正式 A-score 使用 v5，旧开发结果仍对应各自控制器版本。10项 Bob/v5/D 测试通过。开发增益较小，只证明控制器总体能利用正确信息，不保证正式 Qwen 预测一定提高得分。

2026-09-16 正式 A 组0开始后按用户要求暂停。暂停前 `responses.jsonl` 有190条尝试、183条完成：163条合法、20条格式无效；184个不同请求已开始，其中5个发生过传输重试。逐条时间核对显示2个请求等待约300秒后超时，另3个较快失败后重试；不是所有请求都会卡住。暂停时只有 `e4:none:r0` 留下一条未完成 attempt，下次恢复将按已有计数继续，已完成请求不重发。正式 A 尚未形成可评分的完整组结果，不能提前查看条件效果或据此改提示词。
