# AgentResume

项目现已分为两个阶段：

- `Motivation/`：已完成的 Overcooked 动机实验，包含代码、配置、数据、结果和交接文档。
- `MainSearch/`：核心方法、主实验 benchmark 与 baseline 的后续研究。

复现旧实验时，请先进入 `Motivation/`，原有目录之间的相对路径保持不变。
M18 的完整 API 调用记录和中间恢复文件约 800 MB，因此只保存在本地；仓库中保留最终汇总和各组结果。

当前研究入口：[`MainSearch/BENCHMARK_AND_BASELINE.md`](MainSearch/BENCHMARK_AND_BASELINE.md)
