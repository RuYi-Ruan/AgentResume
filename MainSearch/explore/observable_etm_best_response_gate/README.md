# 可观察 ETM 最优响应闭环门槛

每局由两名伙伴执行各自逐渐学习的角色策略，第三名智能体轮流作为响应者。训练时，环境真实执行响应者的三种候选角色并选择出汤最多者作为集中式监督；测试执行时，响应者的协调 MLP 只能读取自己的状态和两条定向 ETM。

所有智能体每三局轮换一次响应者，六条定向 ETM 每局都从可观察轨迹更新。对比 `no_etm`、第 60 局后冻结的 `frozen_etm` 和持续更新的 `dynamic_etm`。

该门槛不使用隐藏角色标签训练 ETM，也不在推理时使用手写角色匹配。低层动作仍由规则执行器完成。
