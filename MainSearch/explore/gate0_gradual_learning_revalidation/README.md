# Gate 0：三智能体渐进学习复验

只验证三名独立 MLP 是否在同一次连续训练中逐渐提高高层分工能力，不涉及 ETM。

- 三套 MLP 从随机初始化开始，训练过程中不加载或切换 checkpoint；
- 使用集中训练、分散执行：中心收益表从真实 Overcooked 对局中形成监督，三名 MLP 各自独立更新；
- 静态对照复制相同初始参数，但全程不更新；
- 每个检查点使用完全相同的固定测试局；
- 同时报告团队出汤数、每名智能体与中心参考分工的一致率，以及单智能体替换后的实际出汤数。

低层动作仍由固定规则执行器完成，因此这里只验证高层分工学习。
