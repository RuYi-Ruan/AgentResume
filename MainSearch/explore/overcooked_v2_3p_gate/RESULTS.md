# 三人三臂图（three_arm_v2）：自举 + 保真协议的结果与结论（2026-09-26）

本目录记录"三人 OvercookedV2 能否学起来"这条线。目标是让伙伴能力在协作中真正演化（≥3 个固定身份、低层原子动作、不能用切换检查点模拟成长），并在该图上做 Oracle 能力门槛与 ETM 对照。

## 1. 地图（`layouts/three_arm_v2.txt`）

外部指导建议"用拓扑拉开单人/三人差距"，据此设计并经静态核验：

- 19×13、107 个可走格；左臂=食材堆、中臂=锅、右臂=盘堆+交付点，底部两行连通，三点固定起点。
- 静态估计（`check_layout_3p.py`）：单人首锅 266–280 步 → 320 步内 **1.14–1.20 份**（满足"单人 ≲1.5"的 P1）。
- 脚本路线实测（`validate_three_arm_route.py`）在修正版地图上**第 234 步完成 1 次正确交付、形状奖励 17、原始奖励 20**。
- 原版 `three_arm` 的缺陷已定位并保留为失败记录：第三个起点 (14,11) 与 B 盘堆堵死通往右臂的唯一通道（路线 221 步走完仍进不去右臂）。修正版把盘堆移进右臂、底部走廊打通。

## 2. 从零 PPO 失败（诊断）

- 三人三臂图、视野半径 3、固定起点、单一菜谱 `[0,0,0]`、320 步、共享 FF MLP（256×256）+PPO，**2.99M 步：训练侧形状奖励 0.0000、正确出汤 0**。
- 短距离对照（2 人 `grounded_coord_simple` **全可见**）3M 步同样 0/0；更早 2 人视野半径 2 跑 20M 步也是 0/0。
- 原因：V2 形状奖励表**没有"取食材"项**，最早的形状事件（放锅/取盘）都要求"手里拿对东西 + 面朝对物体"的合取；随机前馈策略撞不上 → **探索链断在第一环**，不是步数问题。
- 环境与统计已验证正常：脚本路线在同一环境里 234 步得到形状 17 / 原始 20 / 1 次交付。

## 3. 演示预热（BC）——自举成功

- `generate_demos.py`：脚本专家、**6 种角色排列**（两个取料 + 一个送菜，避免 BC 学成"固定角色脚本"）、每步重规划（把队友当动态障碍、显式跟踪朝向，防止窄通道堵死）、扰动（随机起始延迟、2% 随机动作、5% 停顿）。
  - 300 局：**294/300 局有形状奖励、251/300 局至少交付 1 次、平均 0.84 份/局**；216,000 条转移。
- **关键 bug（首次失败的根因）**：第一版把「动作 t」与「观测 t+1」配对。离线指标毫无征兆（总准确率 0.989、交互召回 0.995），但闭环完全为 0。修正配对后离线 macro 0.950、交互召回 1.00。
- `train_bc_ff.py`（均衡批：50% 均匀 + 50% 关键转移）→ `evaluate_bc.py`（jit+vmap 整回合）：
  - 离线：macro 0.950（各类召回 0.87–1.00）；
  - **闭环（采样动作）：95.3% 局有形状奖励、68.8% 局至少 1 次交付、平均 1.13 份/局（单局最多 3）** → 通过"≥75% / ≥25%"门槛。
- **观测混叠诊断**（决定要不要上 GRU）：21.6 万条专家数据上，同一观测的多数动作占比 **0.9898**、条件熵 **0.037 nats**（上限 ln6=1.79）→ **前馈 MLP 足够**，无需 CNN/GRU。
- **已知伪影**：BC 的 **argmax 策略在初始观测上会陷入"停留自环"**（贪心一步不动，故 0 份/局），而同一检查点的采样策略正常（1.13 份/局）。因此**不用 raw argmax 分歧率作核心指标**，改用采样配对收益 + TV/JS 分布距离 + 共同随机数动作切换率（`policy_shift.py`）。

## 4. PPO 保真协议（防灾难性遗忘）

- 直接 BC 权重 + 纯 PPO：第 1 段（9.8 万步）1.00 份/局 → **第 2 段归零**。
- 仅加演示正则 λ(t)·L_BC（λ0=0.5、300 更新衰减）：39 万步稳定，但 49 万步后开始下滑（0.90→0.34），**未能根治**；因为该正则只约束演示状态，而 PPO 漂移发生在自己的状态分布上。
- **最终采用的配方**（`train_overcooked_ff.py` 的 `--bc-init/--bc-regularizer/--critic-warmup-updates/--lr-actor/--lr-critic/--clip-eps/--ent-coef/--ref-kl-beta/--ref-kl-decay-updates`）：
  - 阶段 A（0–0.4M 步 = 100 次更新）：**actor 冻结，只训 critic**；
  - 阶段 B：actor LR 1e-4、critic LR 2.5e-4（`optax.multi_transform` 分组）、clip 0.1、entropy 0.001；
  - 保留演示正则 λ0=1.0 / 600 更新衰减；
  - 新增 **reference-policy KL**：β·KL(π_BC ‖ π_θ) **在 PPO 当前 rollout 状态上**计算，β0=1.0 / 500 更新衰减。
- **1M 门槛结果（通过）**：BC 起点 1.13 份/局 → 冻结期 4 段稳定 1.19；释放后 6 段：0.94 / 1.22 / 1.19 / 0.88 / 0.88 / 0.91（后段均值约 1.0，无系统性下滑）。
- 3M 与 10M 的目标：先确认 ≥2 份/局且正趋势，再上 15–20M（目标 2–4 份/局）。**未达 3M 门槛前不做 Oracle Gate。**

## 5. 下一步（Oracle Gate 设计要点，待实现）

- 混合伙伴能力档位（冻结检查点，仅用于筛选阶段）+ 给观察者注入能力信息。
- **z 输入权重必须零初始化**，保证 π(o, z=0) ≡ π_BC(o)，各臂起点严格一致。
- **critic 输入 = [s_t, z_真_B, z_真_C]，所有臂完全一致**（指导修订：能力门槛阶段若 critic 看不到伙伴档位，同一个物理状态会对应多个回报目标，重新制造 value aliasing；训练期特权信息不是执行期泄漏，因为 actor 侧仍按臂区分）。
- 指标：采样配对收益（3 seeds，paired Δ + bootstrap 95% CI）；`policy_shift.py` 的 TV / JS / 共同随机数切换率（在共享参考状态分布上，参考臂 = 无认知臂）。
- 若发现状态分布不匹配（D_expert ≠ d_π）导致恢复能力不足，优先做 **1 轮 DAgger**（3–8 万条恢复样本），而不是继续加专家演示。

## 4b. CTDE 特权 critic（2026-09-26，解决"PPO 学不动"）

### 诊断

用局部视野 critic（V(o)，1470 维）时，**解释方差 EV ≈ 0**（−0.52 / −0.09 / −0.01 / +0.03 / +0.06），价值误差与收益方差同量级，而 clip 比例 0–2%、每步近似 KL ~5e-4 → **优势基本是噪声**：策略梯度退化为随机游走，叠加熵奖励后熵从 0.18 升到 0.30，锚点（reference-KL / 演示正则）一淡出就崩到 0。21 段锚定期内也没有任何上升趋势。根因：7×7 视野看不到锅内料数/火候、队友手持与朝向，V(o) 在部分可观测下学不出来。

### 实现（`train_overcooked_ff.py`）

- `--privileged-critic`：`MLPActorCriticPriv`，actor 层（Dense_0–2）与 critic 层（Dense_3–5）**完全独立**（不共享 encoder），actor 输入不变；BC 权重按"形状匹配才覆盖"逐层载入（特权 critic 第一层输入维度不同，保留新初始化）。
- **紧凑状态（35 维）**：每智能体 7 维（x/宽、y/高、朝向/4、空手/持盘/持菜/持料 四标志）×3 + 每锅 4 维（料数/3、火候/20、已煮、空闲）+ 食材类型 one-hot + 菜谱 one-hot + 进度 t/T。不用 7410 维全可见观测（静态墙与物体位置每步重复，无信息）。
- `--value-normalization`（默认开）：用 running 均值/方差（count/mean/var 随 runner state 传递）归一化奖励后再做 GAE，使价值目标量级 ≈ 1。
- 新增诊断：`explained_var`、`adv_std`、`adv_return_corr`、`reward_mean/std`、`approx_kl`、`clip_fraction`。

### Gate C0（actor 全程冻结，仅训 critic，0.41M 步）

| 段 | 步数 | EV | 价值损失 | 优势标准差 | corr(A, R) | actor 采样份/局 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 82k | −0.095 | 3.63 | 2.09 | 0.79 | 1.44 |
| 2 | 164k | +0.214 | 2.43 | 1.71 | 0.73 | 1.44 |
| 3 | 246k | +0.463 | 2.20 | 1.62 | 0.70 | 1.44 |
| 4 | 328k | +0.656 | 2.02 | 1.54 | 0.67 | 1.44 |
| 5 | 410k | **+0.747** | 1.80 | **1.48** | 0.65 | 1.44 |

**结论：EV −0.10 → 0.75，优势方差下降约 50%，价值损失减半，actor 完全未动** → 根因判断（局部视野 critic 是瓶颈）得到实验确认。

### 1M 门槛（释放 actor，锚点保留）

| 段 | 步数 | 阶段 | 训练份/局 | EV | 熵 | 采样份/局 |
| --- | --- | --- | --- | --- | --- | --- |
| 1–4 | 0.10–0.41M | 冻结 | 1.08 / 1.16 / 0.99 / 1.14 | −0.03 → 0.77 | 0.19 | 1.19（全程一致） |
| 5–10 | 0.51–1.02M | 释放 | 0.91 / 1.05 / 0.95 / 1.32 / 1.12 / 1.12 | 0.83 → **0.886** | 0.18–0.20 | 0.97 / 1.25 / 0.88 / 0.81 / 0.94 / 0.91 |

判定：**EV 持续 > 0 ✅；无"熵升+收益降"病态 ✅；R_1M ≥ R_BC（1.13）❌**（段均值 ≈0.96，训练侧 ≈1.08）。即灾变性遗忘消失、critic 可用，但 actor 尚未超过 BC（锚点此时仍为 λ≈0.58、β≈0.50）。决定性检验是锚点淡出后的 3M（此前非 CTDE 版本在同一位置崩到 0）。

### 门控顺序（指导修订）

**Critic Gate（0.3–0.5M 只训 critic，看 EV/corr/价值损失）→ 1M → 3M → Oracle Gate**；3M 通过（末 5 段均值 1.4–1.5 或明显上升）后**不再等 10M**，直接用三个不同能力检查点做 Oracle Gate；若 Δ 汤 ≈ 0 就停止，不烧 10M×3 seeds。

## 6. Oracle 能力门槛（三人图，三轮）

### 第 1 轮：用"训练阶段"当能力阶梯 —— 负结果

伙伴 = 同一 CTDE 训练在 98k / 1.47M / 2.95M 步的检查点（实测团队 0.5 / 0.9 / 1.3 份每局）。两臂各 1M 步，配对评测 32 固定局面 × 3 档 = 96 局：

- Δ 团队出汤 = **−0.02 ± 0.09**（采样，配对）
- matched-state **TV = 0.114**、JS = 0.0085、共同随机数切换率 13.6%
- 三档伙伴对结果的影响 1.34 / 0.75 / 1.38（**非单调**）→ 档位本身不构成决策相关的能力差。

### 第 2 轮：角色原型（好/弱 × 取料/服务）—— 仍为负

四个原型由同一脚本规划器生成演示、只记录目标角色智能体的转移（`generate_demos.py --role`），再 BC 成伙伴策略：

| 原型 | BC 测试总准确率 | macro | 交互召回 | 采集时团队出汤/局 |
| --- | --- | --- | --- | --- |
| 取料·强 | 0.938 | 0.940 | 1.000 | 0.90 |
| 取料·弱 | 0.584 | 0.378 | 0.840 | 0.00 |
| 服务·强 | 0.916 | 0.746 | 0.976 | 0.825 |
| 服务·弱 | 0.597 | 0.256 | 0.110 | 0.05 |

门槛用配对 (强取料, 弱服务) 与 (弱取料, 强服务)，两臂各 1M 步：Δ = **+0.03 ± 0.04**、TV = **0.015** → 负。原因见下。

### 关键环境侧证据：最优 ego 角色会随伙伴**翻转**

用脚本角色（能力天然已知、无学习器因素）在 20 局/格上测"ego 选什么角色"：

| ego 角色 | 伙伴 (强取料, 强取料) | 伙伴 (强取料, 强服务) |
| --- | --- | --- |
| **服务** | **0.80**（16/20 局） | 0.20（4/20） |
| **取料** | **0.00**（0/20） | **0.95**（19/20） |

- 伙伴**全是取料型**时，必须有人服务 → ego 服务 = 0.80、ego 取料 = 0.00（**Δ = +0.80**）；
- 伙伴里**已有服务者**时，ego 应去取料 → 0.95 vs 0.20（**Δ = +0.75**）；
- 也就是说：**同样的物理厨房、同样的局面，最优动作随"队友是什么角色"翻转，且值 0.75–0.80 份/局**。这满足"决策相关的角色能力差"判据（≥0.5–0.75 份/局）。

**第 1、2 轮门槛为负是配对选择错误**：那两组配对里"取料"在两种情况下都最优（锅区周转是瓶颈），所以 z 不改变最优动作 → TV 与 Δ 都接近 0。

### 第 3 轮：使用会翻转的配对 —— **首次为正**

配对 = (强取料, 强取料)（此时 ego 必须服务）与 (强取料, 强服务)（此时 ego 应取料），两臂各 **2M 步**，z = 每个伙伴的角色 one-hot。32 固定局面 × 2 配对 = 64 局配对评测（采样动作）：

| 配对 | 无认知 | 给认知 | Δ ± 配对标准误 | t |
| --- | --- | --- | --- | --- |
| (强取料, 强取料) —— 必须服务 | 0.69 | **1.31** | **+0.62 ± 0.24** | **2.60** |
| (强取料, 强服务) —— 应取料 | 0.38 | 0.44 | +0.06 ± 0.13 | 0.47 |
| **总体** | 0.53 | **0.88** | **+0.34 ± 0.14** | **2.43** |

- 总体 **+65%** 团队出汤，配对 t = 2.43；**增益集中在"知道队友角色才能补对位"的配对**（+0.62，t=2.60）；
- 在默认行为本来就正确的配对里没有显著差异（+0.06）→ 与"能力信息只在对位必要处起作用"的机制一致；
- matched-state TV = 0.053、JS = 0.0041、共同随机数切换率 6.3%（行为分布变化适中，收益影响更大）；
- 判据对照（指导）：通过线 Δ ≥ 0.5 且配对 CI > 0 → **总体 +0.34 未达 0.5，但关键配对 +0.62 达标且显著**；强证据线 ≥ 1.0 未达。
- 注意：给认知臂在"必须服务"配对里的 **1.31 份/局已高于脚本最优补位（0.80）**，说明学习到的策略不是简单复制脚本。

**结论（环境侧）**：三人三臂图具备"决策相关的角色能力差"——同一种能力信息会翻转最优角色，补位与不补位的落差 0.75–0.80 份/局（脚本上界），学习器能捕获其中一部分（+0.62）。下一阶段按指导转入**固定身份、独立优化的三个 actor**（主实验的 P2 要求伙伴能力随时间演化）。

### 第 3 轮：3 种子复现（指导要求筛选用 3 seeds）

| seed | 关键配对 (强取料, 强取料) Δ ± 标准误 | (强取料, 强服务) Δ ± 标准误 | 总体 Δ ± 标准误 | 总体 t |
| --- | --- | --- | --- | --- |
| 0 | +0.62 ± 0.24（t=2.60） | +0.06 ± 0.13（t=0.47） | +0.34 ± 0.14 | 2.43 |
| 1 | +0.59 ± 0.24（t=2.46） | +0.12 ± 0.14（t=0.89） | +0.36 ± 0.14 | 2.54 |
| 2 | **+0.97 ± 0.20（t=4.89）** | −0.09 ± 0.11（t=−0.83） | **+0.44 ± 0.13** | **3.33** |
| **均值** | **+0.73** | +0.03 | **+0.38** | ≈2.8 |

- 三个种子在**关键配对**上一致显著为正（+0.59 / +0.62 / +0.97），均值 **+0.73**（达到"通过线 ≥0.5"且 CI > 0；强证据线 ≥1.0 由 seed 2 单独达到）；
- 在"默认行为本来就对"的配对上一致无增益（+0.06 / +0.12 / −0.09）→ 机制特异性得到三次独立确认；
- 贪心（argmax）两臂均为 0（"停留自环"伪影，见 §3），因此门槛一律以**采样动作**判定。

## 7. 独立 actor 训练器的致命 bug（已修复，2026-09-26 晚）

### 症状

三个独立 actor + per-agent CTDE critic 的训练器里：actor 冻结期策略完全正常（团队 1.31 份/局），**actor 一解冻的第一段就崩到 0.00**；同时 `|V|` 从 34 → 216 → 655 发散、BC 损失 0.14 → 252、reference-KL 0 → 22045、熵 0.16 → 0.000。

### 逐项定位（按外部指导的排查顺序）

| 检查 | 结果 | 结论 |
| --- | --- | --- |
| (T,E,A) GAE 合成单元测试（`test_gae_axis.py`，T=4/E=2/A=3，各 agent 奖励尺度 1/10/100，人工 `done` 与 `last_value`） | 与手写双循环逐元素一致（max diff 4.8e-07）；形状不变量成立；**清零 agent 0 奖励后 agent 1/2 优势变化 = 0** | GAE 与 agent 轴处理无问题 |
| 关闭 value normalization | 仍发散 | 归一化不是根因 |
| 冻结 critic | 回报/优势恒定有界（\|V\|≈0.09） | rollout + GAE 管线健康 |
| **代码审查（外部审查发现）** | `tx.update(grads, opt_state, params)` 返回的**已经是参数更新量**，却又被送进 `TrainState.apply_gradients(grads=updates)` —— 后者内部会**再跑一次 Adam**，且用它自己内部重新初始化的 `opt_state` | **双重优化器应用**：符号可能翻转、两套 optimizer state 各自前进 → 同时解释 critic 发散、actor 解冻即崩、锚点反向把策略推离 BC |

### 修复

- scan 的 carry 直接是 `(params, opt_state)`，**删除 `TrainState`**；
- 只应用一次：`upd, new_state = tx.update(grads, opt_state, params)` → `optax.apply_updates(params, upd)`；
- target-KL 用 `jax.lax.cond` 真正跳过（跳过时 optimizer state 也不前进）；
- actor/critic 分组改为显式 `ACTOR_LAYERS = {Dense_0, Dense_1, Dense_2}`（不再依赖字典顺序）；
- 指标改为**逐 agent 记录**（此前只记到最后一个 agent）。

### 回归测试（100 次预热 + 20 次解冻，LRa 1e-5 / LRc 3e-5 / Huber / 关 value norm）

| 检查项 | 修复前 | 修复后 |
| --- | --- | --- |
| 预热期团队出汤 | 1.31 | **1.31** |
| critic 幅度 \|V\| | 34 → 655（发散） | **0.6 → 4.7（缓慢有界）** |
| 解冻后 BC 损失 | 0.14 → 252 | **0.22 → 0.18** |
| reference-KL | 0 → 22,045 | **0 → 0.004** |
| 熵 | 0.16 → 0.000 | **0.15–0.17 稳定** |
| 解冻后团队出汤 | 1.31 → 0.00 | **1.31 → 1.62 → 1.31** |

### 撤回两个在此 bug 下得出的结论

1. "critic 学习率必须降到 3e-5"：当时是在双重 Adam 下测的，只能说明"3e-5 在错误更新链下暂时没炸"；修复后应重新比较 3e-5 / 1e-4 / 2.5e-4。
2. "1470 维观测导致策略对权重过度敏感、需要 obs 归一化"：该测量同样来自错误更新链；BC 已证明当前观测表示足以形成 1.13–1.31 份/局的闭环策略，暂不做输入归一化、不重训 BC。

### 下一步

- 用修好的训练器跑 4.9M 步（lr_actor 1e-5 与 5e-5 各一次），确认长程稳定并观察**伙伴能力是否随经验演化**（P2 的真正问题）。
- 之后再逐步放开 lr_actor（→ 1e-4）与重测 critic LR，最后接 ETM 对照臂（无认知 / 冻结认知 / 在线认知）。

### 修复后的结果：能力真的在涨，且自发分化出角色（P2 成立）

两条 4.9M 步长跑（各 600 次更新，64 环境 × 128 步；LRc 3e-5、Huber、关 value norm）：

| 运行 | 前 5 段均值 | 末 5 段/末段 | 单局最高 | 有交付局数 | 诊断 |
| --- | --- | --- | --- | --- | --- |
| lr_actor 1e-5 | 0.91 | **1.75**（斜率 +0.99/整程，末段 1.72） | 3 | 27/32 | BC 损失 ≤0.22、ref-KL ≤0.011、熵 0.13–0.17、\|V\| 1.3→15 有界 |
| lr_actor 5e-5 | 0.91 | **1.91（末段，31/32 局有交付）** | 3 | 31/32 | 同上，全程无崩溃 |

对照 BC 起点 1.13：**+62%（1e-5）/ +69%（5e-5）**，且超过指导给的 3M 门槛（末 5 段 ≥1.38 且 > 首段）。

**固定身份确实分化出互补角色**（末检查点，24 局独立评测、采样动作，`analyze_independent.py`）：

| 运行 | agent0 交互占比 | agent1 交互占比 | agent2 交互占比 | 两两策略 TV（每步） |
| --- | --- | --- | --- | --- |
| lr 1e-5 | 8.9% | 9.4% | **46.0%** | 0.70 / 0.76 / 0.76 |
| lr 5e-5 | 10.9% | 9.7% | **43.8%** | 0.74 / 0.78 / 0.79 |

即 **agent2 演化为服务者**（交互密集、移动少），**agent0/1 演化为取料者**（交互稀疏、以移动为主），三者策略分布 TV 达 0.70–0.79（上限 1.0）——这正是"固定身份 + 能力随经验演化"所需的 P2 结构，且不是靠切换检查点模拟的。

**这也推翻了此前"shared PPO 从不提升"的结论**：那一结论是在双重优化器 bug 存在时得到的（锚点硬撑、无上升趋势），修复后同一任务上独立 actor 有明确上升。原先 §4b/§4c 中"PPO 学不动"的表述应理解为"训练器有实现缺陷时的表现"。

## 8. 学习型伙伴 Oracle 门槛（2026-09-26 晚）

指导要求：主实验前最后一个必要门槛——用**真实学习伙伴**确认能力信息仍有决策价值（脚本原型只证明"存在某种能力差使信息有价值"）。

### 学习型伙伴的能力画像（`profile_capabilities.py`，24 局/检查点，采样动作）

按事件归因到各身份并归一化到 [0,1]：`c_ingredient`（成功放锅/步）、`c_pot`（装盘/步）、`c_service`（正确交付/步；交付按"曾持菜且不再持菜且有正确交付"归因，因为负奖励会扣减 +20）。

| 检查点 | 团队出汤 | agent0 (c_I/c_P/c_D) | agent1 | agent2 |
| --- | --- | --- | --- | --- |
| lr5e-5 @0.49M（早） | 1.12 | 0.72 / 0.10 / 0.11 | 0.63 / 0.10 / 0.09 | 0.35 / 0.46 / 0.39 |
| lr5e-5 @2.46M（中） | 0.75 | 0.72 / 0.00 / 0.00 | 0.44 / 0.18 / 0.17 | 0.42 / 0.28 / 0.22 |
| lr5e-5 @4.92M（晚） | 1.83 | **0.94 / 0.00 / 0.00** | 0.92 / 0.02 / 0.02 | **0.00 / 1.00 / 0.93** |
| lr1e-5 @4.92M（晚） | 1.92 | **1.00 / 0.00 / 0.00** | 0.92 / 0.00 / 0.00 | **0.06 / 1.00 / 1.00** |

**结论**：早期三个身份都是"样样通"（0.35–0.72），训练后**自发分化成两名纯取料专家 + 一名锅/装盘/交付专家**——能力演化（P2）是由连续学习产生的，不是人工定义。

### 门槛设定与结果（`train_ocv2_learned_gate.py`，z = 两位伙伴的各 3 维能力向量）

两个条件（均用晚期检查点，ego = agent0 学习，伙伴冻结）：
- 条件 0：(agent0 取料专家, agent1 取料专家) → 团队里没人处理锅/交付，ego 理应补服务位；
- 条件 1：(agent0 取料专家, agent2 锅/服务专家) → ego 应去取料。

两臂各 1M 步（64 环境 × 128 步），末 3 段均值：

| 条件 | 无认知 | 给认知 | Δ |
| --- | --- | --- | --- |
| (取料, 取料) | 1.24 | 1.25 | **+0.01** |
| (取料, 锅/服务) | 1.96 | 1.97 | **+0.01** |
| 总体 | 1.60 | 1.61 | +0.01 |

诊断全部健康（熵 0.18、BC 损失 0.15、ref-KL 0.002、单步 KL 0.0002）。

### 解读（重要）

- **伙伴能力差真实存在**（条件间团队成绩差约 0.7 份/局），但**"被告知"在这两个条件下是冗余的**：BC 通才预热的 ego 仅凭局部观测就能识别该补哪个角色（条件 0 无认知臂已达 1.24，高于脚本最优补位的 0.80）。
- 与脚本原型门槛（+0.62/+0.73）不矛盾：脚本伙伴**行为完全固定、绝不解锁第二角色**，ego 必须被"告知"才知道该补位；学习型伙伴的角色**从行为上可辨识**，信息因此冗余。
- 按指导的决策规则（"若 Δ≈0，先检查伙伴能力差是否足够"）：能力差足够 → 这是**真实的"信息冗余"结论**，而非门槛搭建失败。

### 待决：如何让这道门槛有牙齿（或直接进 pilot）

候选：(i) 接受"脚本原型门槛已证明环境结构可行"，直接进 ETM pilot（Frozen vs Online 本身就是"能力随时间变化 ⇒ 过时认知有害"的更强检验）；(ii) 增设**隐藏能力**条件——伙伴在 ego 视野里"风格相同"但能力相差 2–3 倍（例如同一身份的早/中期检查点），使最优角色或投入量依赖**无法从当前观测直接读出**的能力信息；(iii) 视该零效应为本环境的实质性（负）发现。

## 9. 潜在能力门槛（同角色、不同能力、不可即时辨识）——未通过，且有机制解释

按指导先做了两件便宜的事（不先烧 1M Oracle）：

### 9.1 同角色能力阶梯（agent0 的 6 个检查点，24 局/点）

| 检查点 | 团队出汤 | 放锅/步（c_I 原始速率） | c_P | c_D |
| --- | --- | --- | --- | --- |
| 0.49M | 1.12 | 0.0128 | 0.0007 | 0.0007 |
| 1.47M | 1.12 | 0.0139 | 0.0007 | 0.0005 |
| 2.46M | 0.75 | 0.0128 | 0.0000 | 0.0000 |
| 3.44M | 1.38 | 0.0142 | 0.0009 | 0.0007 |
| 4.42M | 1.58 | **0.0160** | 0.0000 | 0.0000 |
| 4.92M | 1.83 | 0.0155 | 0.0000 | 0.0000 |

**同角色能力差只有 1.25×**（0.0128 → 0.0160），低于指导的 1.5–2× 门槛。身份内部的能力梯度在自然训练中很窄；团队成绩的大幅提升来自**角色分化**（取料者 vs 服务者），而不是同一角色的效率增长。

### 9.2 反事实角色测试（同一批初始局面，`latent_capability_test.py`）

团队 = ego（强制脚本角色）+ B（agent0 的 mid 或 late 检查点，同为取料者）+ C（脚本服务者）：

| 组合 | 团队出汤 | 有交付局数 |
| --- | --- | --- |
| B=mid（2.46M）+ ego 强制**取料** | 1.25 | 20/24 |
| B=mid + ego 强制**服务** | 0.00 | 0/24 |
| B=late（4.92M）+ ego 强制**取料** | 1.25 | 18/24 |
| B=late + ego 强制**服务** | 0.00 | 0/24 |

**没有翻转**：ego 的最优角色与伙伴的能力水平无关（mid 与 late 完全同分），只与"服务位是否已被覆盖"有关。

### 9.3 结论（机制性的，不是碰运气）

这张图**取料（长运输链）永远是最紧的瓶颈**——这正是为满足 P1 而刻意设计的拓扑。因此：

- 无论伙伴多强，ego 都应当去取料 ⇒ **能力水平不改变最优角色**；
- 真正改变最优角色的是"伙伴里有没有某个角色"⇒ 这是**直接可观测**的（角色组成）⇒ 给认知（z）冗余（实测 Δ=+0.01）；
- 也就是说：**该任务支持"谁在干哪个角色"的推断（可观测），不支持"他有多强"的潜在能力推断**——而后者才是 ETM 需要追踪的对象。

按指导的判据（同角色能力差 ≥1.5–2×、存在 matched-state 角色翻转），本节两项均未达到 ⇒ **当前任务对 ETM 的长期认知需求不足**，理由有机制支撑。

### 9.4 若要让能力信息重新有决策价值，有两条设计路线（供决策）

1. **让瓶颈随伙伴能力迁移**：目前瓶颈恒为取料。若引入"时间压力/订单节奏"（例如限时内出汤数、或顾客耐心），则当伙伴取料快时，瓶颈会转移到服务/装盘，最优投入随之改变 ⇒ 能力水平影响最优动作。这需要在**规则层**加入时间压力（属于改环境，需你批准）。
2. **同角色能力差放大到 ≥1.5–2×**：用**同角色、真实技能差距**的伙伴（例如取料能力被刻意削弱到 1/3 的同一身份策略——通过训练时限制其有效动作或减少其训练量，而不是加熵噪声），再测是否存在角色翻转。若在 1.5–2× 差距下仍无翻转，则可确定是"瓶颈机制"而非"差距太小"。

## 10. 因果诊断（A：同策略 + 角色动作节流）——阳性

指导定义的 A：**Strong = 已稳定为取料者的 agent0@4.92M**；**Weak = 同一策略**、仅诊断性降低其角色有效动作吞吐（STEAY 节流，角色行为不变）；C 固定为弱服务的 agent2@0.49M；ego 强制两种脚本角色；同一批初始局面、16 局/格。

| B 变体 | ego 取料 | ego 服务 | **D = 服务 − 取料** |
| --- | --- | --- | --- |
| strong（不节流） | 0.62 | 0.94 | **+0.31** ✅ |
| 1.5× 慢（每 3 步强制停留 1 步） | 0.94 | 1.00 | +0.06 |
| **2× 慢（每 2 步强制停留 1 步）** | **0.88** | **0.38** | **−0.50** ✅ |

**结论：D_L·D_H < 0 成立**（−0.50 与 +0.31，均超过 |D|>0.25 的"清晰"标准）⇒ **"同角色能力差足够大时，最优角色确实翻转"得到因果确认**；而自然训练只给出 1.25× 差，未跨过阈值 ⇒ 属"差距太小"这一类。

注：节流同时降低了伙伴在窄通道里的互相阻挡，因此它不是纯净的"能力"操纵（1.5× 档的非单调即由此而来）；它只作为诊断，不进入主实验。

## 11. B 的设计约束（关键算式，供决策）

要让**自然**能力范围内的伙伴差异跨过决策边界，需要"两角色边际成本接近"。设一份汤需要 3 趟取料（3T）与 1 个服务循环（S），则：

- **P1（单人 ≤1.5 份/320 步）** ⇒ 单人每份 ≥213 步 ⇒ `3T + 20 + S ≥ 213`；
- **边界落进自然范围** ⇒ `|3T − S| ≲ 25%·max(3T, S)`（自然同角色差约 1.25×）；
- 两者联立 ⇒ `3T ≈ S ≈ 96` ⇒ **服务循环必须约 96 步**。

但当前 19×13 图上"锅→盘→交付"最长合理循环只有 ≈40–54 步（最大曼哈顿距离 ~30、服务只需一趟）⇒ **做不出来**。把取料腿缩短到能平衡时，单人出汤升到 2.2–2.5 份 ⇒ P1 破坏。**把锅改成 2 份食材也解决不了**：同样的联立给出 `2T ≈ S ≈ 96`，仍要求很长的服务腿。

⇒ 因此 Balanced-Bottleneck 的必要条件是：**服务路线必须足够长（约 90 步）**，例如一条**回环/蛇形**的长服务走廊（路径长度而非曼哈顿距离决定成本），同时取料腿保持短（单程 ~16 步）。这是可行但需要新建地图并重跑整条管线（演示 → BC → 独立 PPO/P2 → 相图 → 门槛，约一天算力）。

### 仍需注意的混淆（针对 §4b/§4c 的旧结论）

我们的 shared-policy PPO **不会让策略超过 BC**（见 §4b/§4c：EV 已修好、无崩溃，但 3M 内无上升趋势）。第 3 轮门槛说明这不妨碍 ego 学到"按 z 切换角色"（+0.62、t=2.60），但在解释**其他配对**的零效应时必须保留这一保留：那里可能是策略无法适应，而不一定只是"能力信息无用"。环境侧的前提另有独立证据（脚本对照表不依赖学习器）。

### 记录：训练奖励曾漏掉 shaping 项（已修）

官方 JaxMARL OvercookedV2 基线的训练奖励是 `reward + anneal_factor · shaped_reward`（系数 1→0，horizon 1.5e7）；我们此前**只用了 raw 奖励**（shaped 仅作日志）→ 取料/锅区角色在自己的奖励里没有任何局部信用。已加入 `--shaped-coeff/--shaped-anneal-updates`，本阶段固定系数 1.0（不退火），评测仍只统计 raw 团队出汤。加上该项后 3M 仍无上升趋势（首 5 段 1.09 → 末 5 段 1.08）。

