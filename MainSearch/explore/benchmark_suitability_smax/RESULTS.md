# SMAX 环境筛选

- 隔离环境：`.venv`，`jax==0.4.36`、`jaxlib==0.4.36`、`jaxmarl==0.2.0`。
- `HeuristicEnemySMAX` + `smacv2_5_units` 重置与单步动作通过：5 名友军、5 名敌军，单人观测长度 127，最长 100 步。
- 随机种子 0 与 1 的单位类型分别为 `[5,3,5,3,5,5,3,5,3,5]` 和 `[5,2,5,3,5,5,2,5,3,5]`；位置也不同。这验证了场景随机性，不验证可学习性或泛化。
- 环境筛选时尚未训练策略或测试 ETM。下一门槛：使用公开基线连续训练至少 3 个种子，固定测试集看胜率曲线与伙伴行为变化；若胜率不稳或 ETM 信息无独立作用，则继续换环境。

## 官方训练基线核对（尚未运行）

- JaxMARL 官方提供 SMAX 的 QMIX RNN 和 PQN-VDN RNN 训练脚本。默认配置是 1000 万环境步；不能直接把它当成本项目的低成本门槛。
- 官方 QMIX RNN 的 `RNNQNetwork` 注明同质智能体参数共享，实际也只维护一份 `agent` 参数；这能测试环境可学习性，但不能证明五套独立策略逐渐学习。
- 官方 QMIX 训练脚本在 `WANDB_MODE=disabled` 时不输出完整测试曲线，且默认只保存最终参数。要检查学习轨迹，需使用本地离线日志或另设记录，不可只看终点胜率。
- 本机 NVIDIA MX450 显存 2 GB；当前 Windows JAX 安装识别为 CPU 版本。不能默认认为这条训练路线可用 GPU 加速。
- 当前隔离环境只安装环境依赖，尚缺训练脚本所需的 `hydra`、`omegaconf`、`flashbax`、`wandb`。尚未启动训练，也没有可报告的胜率。

## 下一道门槛

先做官方基线的小规模可运行性测试，记录真实编译时间、每万步耗时与显存/内存占用；随后再决定是否投入多种子训练。训练达标后，还需改为身份固定的独立策略，并检验跨对局伙伴历史相对当前观察的增益。若训练成本或曲线仍不合适，再筛选 JaxMARL Hanabi 等场景。

已准备 [RUN_SMOKE.md](RUN_SMOKE.md) 的精确命令；首次执行情况见下。

## 8192 步短程首次执行（2026-09-24，失败保留）

- 按 `RUN_SMOKE.md` 安装 JaxMARL v0.2.0 官方源码（提交 `976aeb152cb184a5095021968bba94da96eb6394`）和训练依赖，然后执行指定的 QMIX 命令。进程退出码 1，实际训练环境步数 **0**。
- 失败发生在网络初始化：`AttributeError: module 'jax.api_util' has no attribute 'debug_info'`。当前隔离环境为 JAX/JAXLIB 0.4.36、Flax 0.10.4；该组合在此脚本中不兼容。
- 离线运行记录保留在 `wandb/offline-run-20260924_114357-llml14bc/`；没有生成 `smoke_models/` 参数文件，也没有胜率数据。
- 已见到 JaxMARL 官方仓库的相同报错记录（issue #164）；首次失败时未自动改依赖或重跑，后续经用户同意才开展第二次测试。

## 8192 步短程第二次执行（2026-09-24，失败保留）

- 经用户同意，仅将隔离环境的 Flax 从 0.10.4 改为 0.8.5；JAX/JAXLIB 仍为 0.4.36，训练命令和参数不变。
- 进程再次以退出码 1 在网络初始化阶段失败，实际训练 **0 步**；新报错为 `AttributeError: 'BatchTrace' object has no attribute 'level'`，位于 `flax.core.tracers.trace_level`。
- 离线运行记录保留在 `wandb/offline-run-20260924_115033-jxtesvmz/`；仍无模型和胜率数据。未继续换版本或重跑。
- 两次失败均是依赖兼容性问题，不能归因于 SMAX 场景难学；随后才核对版本并进行第三次测试。

## 8192 步短程第三次执行（2026-09-24，通过）

- 经用户同意核对源码后，将隔离环境设为 JAX/JAXLIB 0.4.36 + Flax 0.10.0；官方 QMIX 命令、种子和参数保持不变。
- 进程正常退出（退出码 0）；离线记录中的最后 `env_step` 为 **8192**。开始约 11:53:16，进程结束约 11:54:36，墙钟约 **80 秒**（含启动、首次编译和保存）。
- 模型参数已保存：`smoke_models/HeuristicEnemySMAX_smacv2_5_units/qmix_rnn_HeuristicEnemySMAX_smacv2_5_units_seed0_vmap0.safetensors`（198,068 字节）；配置 YAML 1,548 字节。离线运行文件在 `wandb/offline-run-20260924_115340-20xj4rei/`。
- 离线记录出现 `test_returned_won_episode`，末尾值为 0；测试完成局数尚未可靠提取。8192 步只是运行性测试，不能据此判定场景可学习性，也没有验证 ETM 或五套独立策略。
- 下一门槛仍是较长连续训练的胜率曲线；但在投入之前，需要确定日志能准确给出各检查点的完成局数与胜局数，且不能用共享参数 QMIX 代表最终方法设定。
