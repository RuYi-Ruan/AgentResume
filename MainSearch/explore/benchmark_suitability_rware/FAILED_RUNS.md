# 失败与警告记录

- 首次 10×2 局四人 smoke 真实完成：停留 10 局合计 0 次配送，随机 10 局合计 1 次配送；但结果写入 JSON 时，NumPy `int64` 无法序列化，进程退出码 1，未生成结果文件。已将动作维度与观察维度显式转为 Python `int`，随后重跑相同种子。此失败属于结果保存脚本，不是环境执行失败。
- Gymnasium 1.3.0 的被动检查器警告环境返回 `list` 类型的四人奖励，而标准单智能体检查器期望标量。是否影响 EPyMARL 训练，须通过其 `gymma` 包装层另行验证；不将警告静默忽略。
- 首次 EPyMARL QMIX 训练在 Sacred 创建日志目录时失败，实际训练 0 步：Gymnasium 环境 ID `rware:rware-tiny-4ag-v2` 的冒号不能用于 Windows 目录名。仅将 `src/main.py` 中日志目录名的冒号替换为下划线后，原命令重跑成功；环境 ID 本身没有修改。
- 首次 10 万步开发试验在 500 环境步、首次保存模型时再次触发相同问题：`src/run.py` 的模型文件名仍含环境 ID 冒号；进程退出码 1，模型未保存，未进入有效训练。已单独修正模型文件名，保留该失败记录后重跑。此前 4000 步不保存模型的短探针已正常完成。
